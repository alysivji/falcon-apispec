# misc
notifications:
  email: false
sudo: false

# python settings
language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"

# install packages
install:
  - pip install -r requirements.txt
  - pip install codecov

# run test
script:
  - python -m pytest --cov=falcon_apispec

after_success:
  - codecov
